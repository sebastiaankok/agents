## Agent skills

### Issue tracker

Issues live in GitHub Issues (`sebastiaankok/agents`). See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary, no overrides. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — `CONTEXT.md` + `docs/adr/` at root. See `docs/agents/domain.md`.
